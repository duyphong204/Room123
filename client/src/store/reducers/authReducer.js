const initStat = {
    isLoggedIn : false ,
    token : null
} 
const authReducer = (state = initStat, action) => {
    switch (action.type) {
      
        default:
            return state;
    }
}
export default authReducer