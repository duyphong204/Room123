const initStat = {
    userData : {}
} 
const userReducer = (state = initStat, action) => {
    switch (action.type) {
      
        default:
            return state;
    }
}
export default userReducer